import { Address, PublicClient, WalletClient } from 'viem';
export type ApprovePrepareTransactionRequestProps = {
    address: Address;
    owner: Address;
    spender: Address;
    amount: bigint;
    publicClient: PublicClient;
};
export declare function approvePrepareTransactionRequest({ address, owner, spender, amount, publicClient, }: ApprovePrepareTransactionRequestProps): Promise<import("viem").PrepareTransactionRequestReturnType>;
export type ApproveProps = {
    address: Address;
    spender: Address;
    amount: bigint;
    publicClient: PublicClient;
    walletClient: WalletClient;
};
export declare function approve({ address, spender, amount, publicClient, walletClient, }: ApproveProps): Promise<import("viem").TransactionReceipt>;
export type FetchAllowanceProps = {
    address: Address;
    owner: Address;
    spender: Address;
    publicClient: PublicClient;
};
export declare function fetchAllowance({ address, owner, spender, publicClient, }: FetchAllowanceProps): Promise<bigint>;
