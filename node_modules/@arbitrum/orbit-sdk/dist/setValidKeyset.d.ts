import { PublicClient, WalletClient } from 'viem';
import { CoreContracts } from './types/CoreContracts';
export type SetValidKeysetParams = {
    coreContracts: Pick<CoreContracts, 'upgradeExecutor' | 'sequencerInbox'>;
    keyset: `0x${string}`;
    publicClient: PublicClient;
    walletClient: WalletClient;
};
export declare function setValidKeyset({ coreContracts, keyset, publicClient, walletClient, }: SetValidKeysetParams): Promise<import("viem").TransactionReceipt>;
